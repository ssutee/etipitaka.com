<!DOCTYPE html>
<html>
<head>
  <title> <%= title %></title>
  <%= yield :head %>
  <%= csrf_meta_tag %>
</head>
<body>

<% if request.fullpath != root_path and request.fullpath != home_path %>
<%= render 'layouts/navigation' %>
<% end %>

<%= yield %>
<%= debug(params) if Rails.env.development? %>
</body>
</html>
